  1. Remove lookup table (mapping_v2.tsv) and build organism mapping from memory
  2. Look into issue #1
  3. Parse BioLisp file for better coordinates -> figure out a way to add extra visual elements relative to excisting coordinates (maybe not needed) 
