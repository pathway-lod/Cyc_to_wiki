# Machine-local config (copy to scripts/config.env)
PLANTCYC_ROOT="$USER/plantcyc/current/plantcyc"
GPML_SNAPSHOT="gpml2021"
OUT_BASE="./output_gpml"
# Increment when re-releasing the same PlantCyc+GPML version with updated code
# e.g. v1, v2, v3 ...
RELEASE_VERSION="v1"